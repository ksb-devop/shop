import React from 'react'
import Head from 'next/head'
import {
    Box,
    Center,
    useColorModeValue,
    Heading,
    Text,
    Stack,
    Image,
    Button,
    Grid,
    Breadcrumb,
    BreadcrumbItem,
    BreadcrumbLink,
  } from '@chakra-ui/react';
  import { useRouter } from 'next/router'
  import Angebot from '../../componenets/Angebot';

const PadMaschine = ({ index, name, url , imageUrl }: PadMaschine) => {

  // eslint-disable-next-line react-hooks/rules-of-hooks
  const router = useRouter(); 
  
  return (
    <div>
       <Head>
        <title>Espresso-Pad-Maschine || Kaffee-Service Berlin Peter Ganss GmbH</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Breadcrumb 
        ml={20} 
        mt={10}
        fontWeight='medium' fontSize='sm'>
        <BreadcrumbItem>
        <BreadcrumbLink href='/maschine'>Büro Maschinen</BreadcrumbLink>
        </BreadcrumbItem>

       <BreadcrumbItem isCurrentPage >
       <BreadcrumbLink href={'/mashcineCategory/jura'}>Espresso-Pad-Maschine  </BreadcrumbLink>
       </BreadcrumbItem>

       </Breadcrumb>


       <Text 
       textAlign={'center'}
       fontSize={{ base: '3xl', md: '3xl', lg: '5xl' }}
       pb={10}
       pt={5}
       >Espresso-Pad-Maschine
       </Text>

      <Center py={12}  >
      <Grid
          
          mb={{ base: 10,  lg: 20 }}
          templateColumns={{ base: 'repeat(1, 1fr)', md: 'repeat(3, 1fr)', lg: 'repeat(4, 1fr)' }} gap={14} w={{ base: '90%', md: '80%', lg: '80%' }}
          >


     {PADM_ITEMS.map((padmaschine) => (

      <Box
        key={padmaschine.index}
        role={'group'}
        p={6}
        maxW={'330px'} 
        w={'full'} 
        // eslint-disable-next-line react-hooks/rules-of-hooks
        bg={useColorModeValue('white', 'gray.800')}
        boxShadow={'dark-lg'}
        rounded={'lg'}
        pos={'relative'}
        zIndex={1}>
        <Box
          rounded={'lg'} 
          pos={'relative'}
          height={'230px'}
           >
          <Image
            alt='jura'
            rounded={'lg'}
            height={230}
            width={282}
            objectFit={'contain'}
            src={padmaschine.imageUrl}
          />
        </Box>
        <Stack pt={10} align={'center'}>
          <Text color={'gray.500'} fontSize={'sm'} textTransform={'uppercase'}>
            {padmaschine.brand}
          </Text>
          <Heading fontSize={'2xl'} fontFamily={'body'} fontWeight={500}>
          {padmaschine.name}
          </Heading>
          <Button 
           onClick={async () => { 
            router.push(padmaschine.url);
  
            }}
          colorScheme='blue'>Mehr Info </Button>
        </Stack>
      </Box>


  ))}

  
</Grid> 
    </Center>

    <Angebot/>

    </div>
  )
}

interface PadMaschine {
    index?: string;
    children?: Array<PadMaschine>;
    name?: string;
    brand?: string;
    url?: string;
    imageUrl?: string;
  }
  
  const PADM_ITEMS: Array<PadMaschine> = [

     
    {
      index: '1',  
      name: 'BLITZ für Espresso & Cappuccino',  
      brand: 'BLITZ',
      url: '/',
      imageUrl: '/padmaschine/pm1.jpg',
       
    },
    {
      index: '2',  
      name: 'Espressopadmaschine',  
      brand: 'Cecilia',
      url: '/',
      imageUrl: '/padmaschine/pm2.jpg',
        
    }, 
    {
      index: '3', 
      name: 'Espressopadmaschine',  
      brand: 'FROG',
      url: '/',
      imageUrl: '/padmaschine/pm3.jpg',
       
    },
    
  ];

export default PadMaschine